from django.apps import AppConfig

class PollSystemConfig(AppConfig):
        default_auto_field = 'django.db.models.BigAutoField'
        name = 'alx_project_nexus.poll_system'  
        # Must match the full path to the app directory